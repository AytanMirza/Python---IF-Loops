# Play the fizz-buzz game: print all numbers between 1 and 100;
# If a number is divisible by 3, print “fizz” instead of the number,
# If it’s divisible by 5, print “buzz” instead of the number,
# If it’s divisible by both, print “fizz buzz” instead of the number.

range = range(1, 101)
for i in range:
    if i % 3 ==0 and i % 5 != 0:
        print("fizz")
    elif i % 5 and i % 3 !=0:
        print("buzz")
    elif i % 5 == 0 and i % 3 == 0:
        print("fizz buzz")
    else:
        print(i)
