# Play the 7-boom game: print all numbers between 1 and 100;
# if a number is divisible by 7 or contains the digit 7, print “BOOM” instead of the number

range = range(1, 101)
for i in range:
    digit = set(str(i))
    if i % 7 == 0 or '7' in digit:
        print("BOOM")
    else:
        print(i)
